<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet id="007-seed-company-user-roles" author="system">
        <comment>Seed EMPLOYER role assignment for employer users (ADMIN has global scope, not company-scoped)</comment>
        
        <!-- NOTE: ADMIN user should NOT have company-scoped roles. ADMIN has GLOBAL scope by design.
             Company roles are only for EMPLOYER and EMPLOYEE users. -->
        
        <!-- This changeset is empty by design - ADMIN role requires no company assignment -->
        <empty/>

        <rollback>
            <empty/>
        </rollback>
    </changeSet>

</databaseChangeLog>
